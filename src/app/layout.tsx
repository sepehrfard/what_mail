export default function Layout({ children }) {
  return (
    <div>
      <header>
        <nav>
          {/* Navigation items */}
        </nav>
      </header>
      <main>
        {children}
      </main>
      <footer>
        {/* Footer items */}
      </footer>
    </div>
  );
}
